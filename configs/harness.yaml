gateway:
  port: 8080
  websocket_port: 18789
  log_level: info
  test_channel: true

agents:
  llm_provider: stub
  stub_response: ""   # empty = echo mode
  mode: conversational

memory:
  type: inmemory
